만약에 까먹었을 때

1. 파일을 새로 만든다 (빈 폴더는 업로드 X)
2. git add .
3. git commit -m "메시지"
4. git push origin main
5. Github에서 업로드 확인

만약 외부 환경에서 한 것을 내려받고 싶다면
git pull (원격저장소명) (브랜치명)
2022년 07월 10일 기준, 여기서는 똑같이
git pull origin main
